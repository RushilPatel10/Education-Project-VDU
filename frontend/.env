# Copy to .env.local in this folder (Create React App loads it automatically).
# Restart "npm start" after any change.

# Use your local API when backend runs on port 5000 with a working MongoDB .env:
# REACT_APP_API_URL=http://localhost:5000

# Leave unset (or delete this file) to use development proxy from package.json → local backend instead.
